#!/usr/bin/expect
# Add key to ssh-agent using passphrase

spawn ssh-add /home/<%= user %>/.ssh/<%= key_name %>
expect "Enter passphrase for /home/<%= user %>/.ssh/<%= key_name %>:"
send "<%= key_passphrase %>\n"
expect eof
exit
